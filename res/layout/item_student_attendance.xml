<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/window_background"
    android:orientation="horizontal"
    android:padding="3dp" >

    <!-- 学生姓名 -->

    <TextView
        android:id="@id/tv_item_student_attendance_name"
        style="@style/item_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:gravity="center_horizontal"
        android:paddingBottom="3dp"
        android:paddingTop="3dp"
        android:text="@string/text_student_name"
        android:textColor="@color/gray_333"
        android:textSize="14sp" />
    <!-- 学生学号 -->

    <TextView
        android:id="@id/tv_item_student_attendance_stunum"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="3"
        android:gravity="center"
        android:layout_gravity="center"
        android:paddingBottom="3dp"
        android:paddingTop="3dp"
        android:text="@string/text_student_stunum"
        android:textColor="@color/gray_333"
        android:textSize="14sp" />

    <!-- 操作 -->

    <Spinner
        android:id="@id/sn_item_student_attendance_type"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
         android:layout_weight="1"
         android:layout_gravity="center"
        android:entries="@array/attendance_types" />

</LinearLayout>